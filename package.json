{
  "name": "footed",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "On-demand, fail-closed pre-filing check for financial affidavits. Emits a disposable Trust Receipt; flags for a human, never rewrites.",
  "scripts": {
    "selftest": "node --experimental-strip-types engine/selftest.ts",
    "receipt": "node --experimental-strip-types receipt/emit.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
